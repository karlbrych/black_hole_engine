[package]
name = "texture-preprocessor"
version = "0.1.0"
edition = "2024"

[dependencies]
image = {version="0.25", features = ["png","jpeg"]}
anyhow = "1"
clap = { version = "4", features = ["derive"] }