#include "shader.h"
#include <fstream>
#include <sstream>
#include <iostream>
shader::shader(const char* vertexPath,const char* fragmentPath){
	//dostani shaderu z files
	std::string vertexCode;
	std::string fragmentCode;
	std::ifstream vShaderFile;
	std::ifstream fShaderFile;
}